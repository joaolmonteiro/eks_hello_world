apiVersion: v1
kind: Service
metadata:
  name: frontend2-service
  namespace: hello-world-preprod
  labels:
    app: frontend2
    environment: preprod
spec:
  selector:
    app: frontend2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
